framework:
    secret: %secret%
    router:
        resource: %kernel.root_dir%/config/routing.yml
    templating:
        engines: [twig]
    assets: ~
    profiler:
        enabled: %kernel.debug%

twig:
    paths:
        '%kernel.root_dir%/src/AppBundle/Views': ''

